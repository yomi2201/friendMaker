<%= form_with(model: original_question, local: true) do |form| %>
  <% if original_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(original_question.errors.count, "error") %> prohibited this original_question from being saved:</h2>

      <ul>
      <% original_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :question_id %>
    <%= form.number_field :question_id %>
  </div>

  <div class="field">
    <%= form.label :original_question %>
    <%= form.text_field :original_question %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
